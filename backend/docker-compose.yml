services:
  cloudflared:
    image: cloudflare/cloudflared:latest
    container_name: aegis-edge-tunnel
    restart: unless-stopped
    command: tunnel --url http://localhost:8000
    network_mode: host